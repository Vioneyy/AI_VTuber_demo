# Terminal#1290-1383 Notes

สถานะและโน้ตสำหรับการทดสอบล่าสุด (ตามที่ขอ):

- RVC model not found: โฟลเดอร์ `D:\AI_VTuber_demo\rvc_models` ว่างเปล่า จึงหาไฟล์ `.pth` ไม่เจอ ให้จัดวางไฟล์ `.pth` ไว้ที่พาธถูกต้อง หรือแก้ค่า `RVC_MODEL_PATH` ใน `.env` ให้ตรงกับไฟล์จริง
- โมเดลขยับเล็กน้อยเกินไป: อยากให้เพิ่มความแรงการขยับ และสุ่มความแรงของการขยับเพื่อให้ดูมีชีวิตชีวา
- ใช้คำสั่งใน Discord Bot แล้วโมเดลนิ่งไม่ขยับ: หลังใช้คำสั่งบางอย่าง โมเดลหยุดขยับไปเลย
- ทดลองใช้ `!join` และ `!stt 5` แล้วบอทตอบว่า `❌ หนูต้องอยู่ในห้องเสียงก่อน! ใช้ !join` ทั้งที่อยู่ในห้องเสียงแล้ว

สิ่งที่ทำในรอบนี้:

- ปรับไฟล์ `.env.example` ให้มีโครงสร้างและค่าที่ไม่ใช่ token ตรงกับไฟล์ `.env`
- ไม่ใส่ค่า token ใด ๆ ใน `.env.example`
- อัปโหลดไฟล์ทั้งหมดที่สามารถอัปได้ขึ้น GitHub: `https://github.com/Vioneyy/AI_VTBER.git`
- หมายเหตุ: เนื่องจากข้อจำกัดของ GitHub (ไฟล์ใหญ่เกิน 100MB) จึงไม่ได้อัปโหลดไฟล์โมเดลใหญ่อย่าง `models/ggml-large-v3.bin` และได้เพิ่มข้อยกเว้นใน `.gitignore` สำหรับไฟล์โมเดลขนาดใหญ่ (`*.bin`, `*.pth`, `*.onnx`, `*.pt`, `*.ckpt`)

หมายเหตุเพิ่มเติม:

- `.env` ถูก ignore โดย `.gitignore` เพื่อไม่ให้ข้อมูลลับถูก commit
- สามารถแมป Custom Parameters จากปลั๊กอินไปยังพารามิเตอร์ของโมเดลใน VTube Studio เพื่อให้การขยับมีผลจริง