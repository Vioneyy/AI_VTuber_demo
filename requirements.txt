# ==========================================
# Jeed AI VTuber - Python Dependencies (แก้ไขเวอร์ชันเสถียร)
# ==========================================

# Core
python-dotenv==1.0.1

# Discord (ใช้ PyCord + Voice)
py-cord==2.5.0
PyNaCl==1.5.0

# OpenAI
openai>=1.30.0

# Audio Processing (แก้ NumPy binary incompatibility)
numpy==1.26.4  # เวอร์ชันเสถียรบน Windows; ลด error dtype size changed
soundfile==0.12.1
noisereduce==3.0.0
scipy==1.11.4

# TTS (F5-TTS-Thai / Torch)
# หมายเหตุ: ติดตั้ง Torch/Audio ตามแพลตฟอร์ม (CPU/GPU) ให้เข้ากัน
torch
torchaudio
transformers==4.43.3
# f5-tts - แนะนำติดตั้งแยกจาก git:
# pip install git+https://github.com/DomhnallBoyle/f5-tts.git

# RVC (เพิ่ม rvc-python)
# ติดตั้งแยกจาก git เพื่อรองรับโมดูล RVC:
# pip install git+https://github.com/RVC-Project/Retrieval-based-Voice-Conversion-WebUI.git

# YouTube Live
pytchat==0.5.5

# VTube Studio / WebSocket
websockets==12.0

# Utilities
aiohttp==3.9.5
pydantic==2.9.2